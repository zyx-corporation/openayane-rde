# citation_deleted (Markdown)

**Scenario:** Preservation contract; inline citations in the Background section are removed while prose is smoothed.

**Corruption type:** Citation deletion / protected-element break.

**Expected RDE:** `critical_corruption` → advisory `required_action` `halt` (see `expected_rde_result.json`).

**Golden lineage:** Derived from `tests/golden/fixtures/markdown_citation_deleted` (content aligned; `contract_id` is benchmark-specific).
